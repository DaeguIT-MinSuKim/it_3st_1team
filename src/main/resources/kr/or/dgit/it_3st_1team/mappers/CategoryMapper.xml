<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.dgit.it_3st_1team.dao.CategoryDao">

<<<<<<< HEAD
	<!-- 대분류 콤보박스 설정 -->
	<sql id="selectBigCategorySql">
		select num, catename, parentnum from category where catesize = 0
	</sql>
	
	<select id="selectBigCategoryByAllWithAPI" parameterType="Category" resultType="Category">
		<include refid="selectBigCategorySql" />
	</select>
	
	<!-- 중분류 콤보 박스 설정 -->
	<sql id="selectMidCategorySql">
		select num, catename from category where catesize = 1
	</sql>
	
	<sql id="whereparentnumSql">
			<if test="parentnum != null">
				and parentnum = #{parentnum}
			</if>
	</sql>
	
	<select id="selectMidCategoryByAllWithAPI" parameterType="Category" resultType="Category">
		<include refid="selectMidCategorySql" />
		<include refid="whereparentnumSql" />
	</select>
	
	
	<!-- 대분류 이름으로 넘버 불러오기 -->
	
	<sql id="selectCategoryAllSql">
		select num, catename, parentnum from category
	</sql>
	
	<sql id="whereNameSql">
		<where>
			<if test="catename != null">
				catename = #{catename}
			</if>
		</where>
	</sql>
	
	<select id="selectCategoryByNameWithAPI"  parameterType="Category" resultType="Category">
		<include refid="selectCategoryAllSql" />
		<include refid="whereNameSql" />
	</select>
=======
	<resultMap type="Category" id="CategoryResult">
		<id property="num" column="num"/>
		<result property="catename" column="catename"/>
		<result property="catesize" column="catesize"/>
		<result property="parentnum" column="parentnum"/>
	</resultMap>
	
	<select id="selectCategoryBig" parameterType="Category" resultMap="CategoryResult">
		select num, catename, catesize, parentnum from category where catesize = 0 order by num
	</select>
	
	<select id="selectCategoryMid" parameterType="Category" resultMap="CategoryResult">
		select num, catename, catesize, parentnum from category 
		where catesize = 1 and parentnum = #{parentnum} order by num
	</select>
	
>>>>>>> refs/remotes/origin/master
</mapper>